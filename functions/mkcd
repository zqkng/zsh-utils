###############################################################################
# function: mkcd
# ---------------
# Create one (`dir/`)  or multiple new directories (`a/b/c/`)
#   and then `cd` into the new directory
#
###############################################################################

mkcd() {
    mkdir -p "$1" && cd "$1";
}
